week 7
import boto3

table = boto3.resource('dynamodb').Table('newtable2')

def lambda_handler(event, context):
    print("TRIGGERED")
    
    for r in event['Records']:
        table.put_item(Item={
            'Object': r['s3']['object']['key']
        })


week 8
def lambda_handler(event, context):
    for record in event['Records']:
        print("Message from SQS:")
        print(record['body'])
